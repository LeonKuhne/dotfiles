#
# ~/.fish_profile
#

if test -e ~/.bashrc
  bash -c ". ~/.bashrc"
end

if test -e ~/.xinitrc
  bash -c ". ~/.xinitrc"
end

set -Ux BEMENU_BACKEND wayland
set -Ux EDITOR vim
set -Ux QT_QPA_PLATFORMTHEME qt5ct
set -Ux ANDROID_HOME /home/id/Android/Sdk

echo "runing fish_profile"
